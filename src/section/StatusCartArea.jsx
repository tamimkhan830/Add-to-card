import StatusCart from "./StatusCart"


function StatusCartArea() {
  return (
    <div>
        <StatusCart bgColor = "bg-green-500" />
        <StatusCart bgColor = "bg-gray-500" />
        <StatusCart bgColor = "bg-rose-500" />
    </div>
  )
}

export default StatusCartArea
